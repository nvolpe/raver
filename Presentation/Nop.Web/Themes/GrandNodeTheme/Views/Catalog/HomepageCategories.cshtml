@model IList<CategoryModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    <div class="category-grid home-page-category-grid">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col s12 m4">
                    <div class="card medium hoverable center-align">                        
                        <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                            <img alt="@item.PictureModel.AlternateText" class="responsive-img max-height-img" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                        </a>
                        <div class="card-content center-align">
                            <a class="card-title grey-text text-darken-4" href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                @item.Name
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}